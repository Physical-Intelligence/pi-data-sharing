# Core dependencies
pandas>=1.3.0
numpy>=1.20.0
jsonschema>=4.0.0
tyro>=0.5.0
cloudpathlib[gs]>=0.15.0

# Pandas dependencies (explicit for clarity)
python-dateutil>=2.8.0
pytz>=2021.1

# Google Cloud Storage support (for cloudpathlib[gs])
google-cloud-storage>=2.0.0

# CLI and utilities
rich>=13.0.0  # For tyro's rich output
